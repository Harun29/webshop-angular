import { StopScrollPropagationDirective } from './stop-scroll-propagation.directive';

describe('StopPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new StopScrollPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
